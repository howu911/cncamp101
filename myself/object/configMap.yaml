apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
data:
  default.conf: |
    server {
        listen 80;
        location / {
            default_type text/plain;
            return 200 "Hello from Pod: $hostname\n";
        }
    }